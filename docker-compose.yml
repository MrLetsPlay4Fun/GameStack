services:
  backend:
    build: ./backend
    container_name: gamestack-backend
    restart: unless-stopped
    # host-Netzwerk: alle Game-Server-Ports sind direkt am Host erreichbar
    network_mode: host
    environment:
      PORT: 3001
      JWT_SECRET: ${JWT_SECRET:-change-this-secret-in-production}
      DATABASE_URL: file:/data/gamestack.db
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      SERVERS_PATH: /data/servers
      BACKUPS_PATH: /data/backups
    volumes:
      # Datenbank persistieren
      - gamestack-data:/data
      # Game-Server-Daten persistieren (außerhalb des Containers)
      - ${SERVERS_PATH:-./data/servers}:/data/servers
      - ${BACKUPS_PATH:-./data/backups}:/data/backups

  frontend:
    build: ./frontend
    container_name: gamestack-frontend
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-80}:80"
    depends_on:
      - backend
    # Erlaubt nginx den Zugriff auf das Backend im host-Netzwerk
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - gamestack

volumes:
  gamestack-data:

networks:
  gamestack:
    driver: bridge
